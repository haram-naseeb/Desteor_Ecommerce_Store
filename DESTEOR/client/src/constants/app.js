/**
 * constants/app.js
 *
 * Small, static values referenced across the app that aren't routes.
 * Grows as real features (Sprint 2+) need shared constants
 * (e.g. order status enums, pagination defaults).
 */

export const APP_NAME = 'DESTEOR';
export const APP_TAGLINE = 'Crafted for Your Destiny.';
