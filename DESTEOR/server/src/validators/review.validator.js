function validateReview(req, res, next) { const { rating, title, comment } = req.body; const errors = {}; if (!Number.isInteger(rating) || rating < 1 || rating > 5) errors.rating = 'Rating must be between 1 and 5.'; if (!title?.trim() || title.trim().length > 120) errors.title = 'Title is required and must be 120 characters or fewer.'; if (!comment?.trim() || comment.trim().length > 2000) errors.comment = 'Comment is required and must be 2,000 characters or fewer.'; if (Object.keys(errors).length) return res.status(422).json({ success: false, message: 'Validation failed.', errors }); next(); }
module.exports = { validateReview };
